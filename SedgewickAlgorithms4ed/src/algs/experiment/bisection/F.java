package algs.experiment.bisection;

/**
 * Interface for a function that computes f(x) for a double x.
 */
public interface F {

	/** Compute and return f(x). */
	double compute(double x);
}
